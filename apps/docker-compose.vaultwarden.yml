version: "3.9"

# Stack de cofres/segredos (Vaultwarden compatível com Bitwarden).
services:
  vaultwarden:
    image: vaultwarden/server:alpine
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      - DOMAIN=https://pw.${HOMELAB_DOMAIN:-example.local}
      - WEBSOCKET_ENABLED=true
      # Define token para acessar /admin e finalizar configuração inicial
      - ADMIN_TOKEN=${VAULTWARDEN_ADMIN_TOKEN:-changeme}
      # Permite o cadastro do primeiro usuário/admin ao subir a stack
      - SIGNUPS_ALLOWED=${VAULTWARDEN_SIGNUPS_ALLOWED:-true}
      # Configura SMTP para envio de OTP por e-mail (2FA)
      - SMTP_HOST=${VAULTWARDEN_SMTP_HOST:-smtp.example.local}
      - SMTP_FROM=${VAULTWARDEN_SMTP_FROM:-vaultwarden@example.local}
      - SMTP_PORT=${VAULTWARDEN_SMTP_PORT:-587}
      - SMTP_SSL=${VAULTWARDEN_SMTP_SSL:-false}
      - SMTP_USERNAME=${VAULTWARDEN_SMTP_USERNAME:-vaultwarden}
      - SMTP_PASSWORD=${VAULTWARDEN_SMTP_PASSWORD:-changeme}
      - SMTP_TIMEOUT=${VAULTWARDEN_SMTP_TIMEOUT:-15}
      - SMTP_EXPLICIT_TLS=${VAULTWARDEN_SMTP_EXPLICIT_TLS:-true}
      - LOG_LEVEL=info
    volumes:
      - /srv/homelab/vaultwarden/data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vaultwarden.rule=Host(`pw.${HOMELAB_DOMAIN}`)"
      - "traefik.http.routers.vaultwarden.entrypoints=websecure"
      - "traefik.http.routers.vaultwarden.tls=true"
      - "traefik.http.routers.vaultwarden.tls.certresolver=letsencrypt"
      - "traefik.http.services.vaultwarden.loadbalancer.server.port=80"
    networks:
      - proxy_net
      - internal_net

networks:
  proxy_net:
    external: true
    name: ${PROXY_NETWORK}
  internal_net:
    external: true
    name: ${INTERNAL_NETWORK}
